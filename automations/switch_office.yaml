
# -------------------------------- Button left ------------------------------- #

- id: SWL-OFFC-001
  alias: Kantoor lampen aan status
  trigger:
    platform: state
    entity_id: switch.office_left
    to: 'on'
  action:
  - service: homeassistant.turn_on
    entity_id: light.kantoor_spots

- id: SWL-OFFC-002
  alias: Kantoor lampen uit status
  trigger:
    platform: state
    entity_id: switch.office_left
    to: 'off'
  action:
  - service: homeassistant.turn_off
    entity_id: light.kantoor_spots

- id: SWL-OFFC-003
  alias: Kantoor schakelaar aan status
  trigger:
    platform: state
    entity_id: light.kantoor_spots
    to: 'on'
  action:
  - service: homeassistant.turn_on
    entity_id: switch.office_left

- id: SWL-OFFC-004
  alias: Kantoor schakelaar uit status
  trigger:
    platform: state
    entity_id: light.kantoor_spots
    to: 'off'
  action:
  - service: homeassistant.turn_off
    entity_id: switch.office_left

# ------------------------------- Button middle ------------------------------ #

- id: SWM-OFFC-001
  alias: Kantoor sfeerspots aan status
  trigger:
    platform: state
    entity_id: switch.office_middle
    to: 'on'
  action:
  - service: homeassistant.turn_on
    entity_id: switch.kantoor_sfeerspots

- id: SWM-OFFC-002
  alias: Kantoor sfeerspots uit status
  trigger:
    platform: state
    entity_id: switch.office_middle
    to: 'off'
  action:
  - service: homeassistant.turn_off
    entity_id: switch.kantoor_sfeerspots

- id: SWM-OFFC-003
  alias: Kantoor sfeerspots schakelaar aan status
  trigger:
    platform: state
    entity_id: switch.kantoor_sfeerspots
    to: 'on'
  action:
  - service: homeassistant.turn_on
    entity_id: switch.office_middle

- id: SWM-OFFC-004
  alias: Kantoor sfeerspots schakelaar uit status
  trigger:
    platform: state
    entity_id: switch.kantoor_sfeerspots
    to: 'off'
  action:
  - service: homeassistant.turn_off
    entity_id: switch.office_middle

# ------------------------------- Button right ------------------------------- #

- id: SWR-OFFC-001
  alias: Salon lampen aan status
  trigger:
    platform: state
    entity_id: switch.office_right
    to: 'on'
  action:
  - service: homeassistant.turn_on
    entity_id: light.studio_spots

- id: SWR-OFFC-002
  alias: Salon lampen uit status
  trigger:
    platform: state
    entity_id: switch.office_right
    to: 'off'
  action:
  - service: homeassistant.turn_off
    entity_id: light.studio_spots

- id: SWR-OFFC-003
  alias: Salon schakelaar aan status
  trigger:
    platform: state
    entity_id: light.studio_spots
    to: 'on'
  action:
  - service: homeassistant.turn_on
    entity_id: switch.office_right

- id: SWR-OFFC-004
  alias: Salon schakelaar uit status
  trigger:
    platform: state
    entity_id: light.studio_spots
    to: 'off'
  action:
  - service: homeassistant.turn_off
    entity_id: switch.office_right
